name: First CLI

on:
  push:
  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running unit tests"

  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building project"
      
  
  
