name: First CLI

on:
  push:
    branches:
      - main
  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running unit tests

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building project"
      
  
  
